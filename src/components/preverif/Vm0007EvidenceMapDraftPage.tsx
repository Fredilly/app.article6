"use client";
import { useEffect, useState } from "react";
import { loadVm0007EvidenceMapDraft } from "@/lib/preverif/vm0007EvidenceMapDraftStore";
import type { Vm0007EvidenceMapDraftPackage } from "@/lib/preverif/vm0007EvidenceMapDraft";
import { approveVm0007EvidenceMapRow, editVm0007EvidenceMapRow, finalizeVm0007EvidenceMap, reopenVm0007EvidenceMapRow } from "@/lib/preverif/vm0007EvidenceMapReview";

function humanizeRawAuditStatus(status: string): string {
  switch (status) {
    case "supported_by_pdd": return "Supported by PDD";
    case "partially_supported": return "Partially supported";
    case "missing_evidence": return "Missing evidence";
    case "not_applicable": return "Not applicable";
    case "manual_review_needed": return "Manual review needed";
    default: return status;
  }
}

export default function Vm0007EvidenceMapDraftPage({ auditId }: { auditId: string }) {
  const [pkg, setPkg] = useState<Vm0007EvidenceMapDraftPackage | null>(null);
  const [message, setMessage] = useState<string | null>(null);
  useEffect(() => setPkg(loadVm0007EvidenceMapDraft(auditId)), [auditId]);
  if (!pkg) return <main className="min-h-screen bg-slate-50 p-8"><div className="mx-auto max-w-4xl rounded-2xl border border-amber-200 bg-white p-6 text-amber-900">Evidence Map is not available for this audit. Only a valid VM0007 v1.8 draft can be opened.</div></main>;
  const counts = pkg.rows.reduce<Record<string, number>>((all, row) => ({ ...all, [row.rawAuditStatus]: (all[row.rawAuditStatus] ?? 0) + 1 }), {});
  const allApproved = pkg.rows.every((row) => row.reviewState === "approved");
  const finalize = () => {
    const result = finalizeVm0007EvidenceMap(pkg, "reviewer:local");
    if (result.ok) { setPkg(result.package); setMessage("Evidence Map finalized. The readiness report is now available."); }
    else setMessage(`Finalization blocked: ${result.blockedBy.join("; ")}`);
  };
  const review = (rowId: string, action: "approve" | "reopen" | "edit") => {
    const current = pkg.rows.find((row) => row.rowId === rowId);
    if (!current) return;
    const result = action === "approve"
      ? approveVm0007EvidenceMapRow(pkg, rowId, "reviewer:local")
      : action === "reopen"
        ? reopenVm0007EvidenceMapRow(pkg, rowId, "reviewer:local")
        : editVm0007EvidenceMapRow(pkg, rowId, { assessmentReason: window.prompt("Assessment reason", current.assessmentReason) ?? current.assessmentReason }, "reviewer:local");
    if (result.ok) { setPkg(result.package); setMessage(`Row ${rowId} is now ${result.row.reviewState}.`); }
    else setMessage(`Review action blocked: ${result.reason}`);
  };
  return <main className="min-h-screen bg-slate-50 px-4 py-8"><div className="mx-auto max-w-7xl"><div className={`mb-5 rounded-2xl border p-5 text-sm font-semibold ${pkg.finalizationState === "finalized" ? "border-emerald-300 bg-emerald-50 text-emerald-950" : "border-amber-300 bg-amber-50 text-amber-950"}`}>{pkg.finalizationState === "finalized" ? "Finalized Evidence Map. Reopening or editing a row requires re-finalization." : "Machine-proposed Evidence Map. These rows have not been reviewer-finalized. Review each row before finalization."}</div><section className="mb-5 rounded-2xl border border-slate-200 bg-white p-5"><h1 className="text-xl font-semibold text-slate-950">Evidence Map</h1><p className="mt-2 text-sm text-slate-600">{pkg.methodologyId} {pkg.rulebookVersion} · {pkg.rows.length} requirements</p><p className="mt-2 text-sm text-slate-600">Source: {pkg.sourceDocument.documentName || pkg.sourceDocument.documentId} · Audit ID: {pkg.auditId}</p><p className="mt-2 text-sm text-slate-600">Source audit status: {Object.entries(counts).map(([status, count]) => `${humanizeRawAuditStatus(status)} ${count}`).join(" · ")}</p><p className="mt-2 text-sm text-slate-600">Pre-Validation Readiness Report: available after the Evidence Map has been reviewer-finalized.</p><button type="button" onClick={finalize} disabled={!allApproved || pkg.finalizationState === "finalized"} className="mt-4 rounded-full border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-semibold text-white disabled:border-slate-200 disabled:bg-slate-100 disabled:text-slate-500">Finalize Evidence Map</button></section><div className="space-y-4">{pkg.rows.map((row) => { const showTitle = row.ruleReference.trim().toLowerCase() !== row.ruleTitle.trim().toLowerCase(); return <article key={row.rowId} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex flex-wrap items-baseline justify-between gap-2"><h2 className="font-semibold text-slate-950">{showTitle ? `${row.ruleReference} · ${row.ruleTitle}` : row.ruleReference}</h2><span className="rounded-full border border-slate-300 px-2 py-1 text-xs font-semibold">{humanizeRawAuditStatus(row.rawAuditStatus)} · {row.reviewState}</span></div><p className="mt-2 text-sm text-slate-600">Proposed evidence: {row.proposedEvidenceStatus} · Applicability: {row.proposedApplicability} · Confidence: {row.confidence}</p><dl className="mt-3 grid gap-3 text-sm md:grid-cols-2"><div><dt className="font-semibold text-slate-700">Requirement</dt><dd className="mt-1 text-slate-600">{row.requirementText}</dd></div><div><dt className="font-semibold text-slate-700">Proposed candidate evidence</dt><dd className="mt-1 text-slate-600">{row.proposedAcceptedEvidence?.quote || "None proposed"}</dd></div><div><dt className="font-semibold text-slate-700">Proposed rejected or uncertain evidence</dt><dd className="mt-1 text-slate-600">{row.proposedRejectedEvidence ? `${row.proposedRejectedEvidence.quote} — ${row.proposedRejectedEvidence.reason}` : "None proposed"}</dd></div><div><dt className="font-semibold text-slate-700">Quote / location</dt><dd className="mt-1 text-slate-600">{row.quote || "No quote"} · page {row.page ?? "—"} · {row.section || "No section"}</dd></div><div><dt className="font-semibold text-slate-700">Assessment / client action</dt><dd className="mt-1 text-slate-600">{row.assessmentReason} {row.clientAction}</dd></div><div className="md:col-span-2"><dt className="font-semibold text-slate-700">Provenance</dt><dd className="mt-1 text-slate-600">{row.provenance ? `${row.provenance.docId} · ${row.provenance.spanId} · ${row.provenance.sectionHeading || "No section"}` : "No provenance proposed"}</dd></div></dl><div className="mt-4 flex flex-wrap gap-2 border-t border-slate-100 pt-3"><button type="button" onClick={() => review(row.rowId, "approve")} disabled={row.reviewState === "approved" || pkg.finalizationState === "finalized"} className="rounded-lg border border-emerald-300 px-3 py-1.5 text-xs font-semibold text-emerald-900 disabled:opacity-50">Approve</button><button type="button" onClick={() => review(row.rowId, "edit")} disabled={pkg.finalizationState === "finalized"} className="rounded-lg border border-blue-300 px-3 py-1.5 text-xs font-semibold text-blue-900 disabled:opacity-50">Edit</button><button type="button" onClick={() => review(row.rowId, "reopen")} disabled={pkg.finalizationState !== "finalized" && row.reviewState !== "approved" && row.reviewState !== "edited"} className="rounded-lg border border-amber-300 px-3 py-1.5 text-xs font-semibold text-amber-900 disabled:opacity-50">Reopen</button></div><div className="mt-2 text-xs text-slate-500">Review history: {row.reviewHistory?.length ?? 0} event(s){row.reviewHistory?.length ? ` · last by ${row.reviewHistory[row.reviewHistory.length - 1].reviewerIdentity}` : ""}</div></article>; })}</div><div className="mt-8 rounded-2xl border border-slate-200 bg-white p-5"><button type="button" onClick={finalize} disabled={!allApproved || pkg.finalizationState === "finalized"} className="rounded-full border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-semibold text-white disabled:border-slate-200 disabled:bg-slate-100 disabled:text-slate-500">Finalize Evidence Map</button>{pkg.finalizationState === "finalized" ? <a href={`/quick-check/pre-validation-readiness?auditId=${encodeURIComponent(pkg.auditId)}`} className="ml-3 inline-block rounded-full bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Open Pre-Validation Readiness Report</a> : <p className="mt-2 text-sm text-slate-600">Available after the Evidence Map has been reviewer-finalized. All rows must be approved before the map can be finalized.</p>}{message ? <p className="mt-3 text-sm text-slate-700" role="status">{message}</p> : null}</div></div></main>;
}
