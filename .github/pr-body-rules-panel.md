## Summary
- improve Rules tab rows so rule ID, title, preview text, tags, and activity hints are easier to scan
- surface linked-workspace and evidence/review counts directly in the list
- keep the change scoped to rules panel information density only

## Validation
- `npm run typecheck`
- `npm test`, `npm run lint`, and `npm run build` were not runnable locally on this machine because the required Next SWC darwin/x64 binary is missing
