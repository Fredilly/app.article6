# Quick Check v2 fixture review: Lisala REDD+ Project

`gold.draft.json` is the raw Quick Check output from intake. `gold.json` is the reviewed PDF truth.

Review outcome:

- `host_country` is `FOUND` as `Democratic Republic of Congo`, not `Mongala`.
- `methodology` is `FOUND` from the formal page 46 methodology table.
- `baseline_scenario` is `UNCLEAR`. The PDD narrates a without-project scenario, but the formal baseline section is under development.
- `additionality` is `UNCLEAR`. The additionality section is under development, so the without-project narrative does not prove additionality.
- `leakage` is `UNCLEAR`. Leakage sections are under development, so template text is not proof.
- `stakeholder_consultation` is `UNCLEAR`. Broad stakeholder language is not enough; the detailed consultation sections are under development.

Rules applied:

- Under-development sections are not accepted as `FOUND`.
- Broad narrative about project context does not substitute for a completed baseline, additionality, leakage, or consultation section.
- `host_country` must identify the country, not the province.
