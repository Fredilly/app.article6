# Frozen baselines

This directory contains immutable starting states for each roadmap release cycle. Baselines include frozen machine proposals, audit artifacts, baseline manifests, and the hashes needed to reproduce and verify them. Baseline contents must not be rewritten as review cycles progress.
