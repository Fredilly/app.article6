# Review-cycle artifacts

This directory contains execution history for each roadmap release cycle. It holds review packets, adjudication responses, reviewed truth, batch artifacts, comparisons, reports, and other outputs produced during review. Each cycle is organized under its `rcN/` directory.
