# UX / Workflow Backlog

This backlog stores **nice-to-have** UX and workflow improvements that should **not** pollute the verifier moat/flywheel roadmap.

- Flywheel roadmaps = moat primitives only.
- UX/workflow ideas live here as `UX-xxxx`.
- Items are promoted into a PR only when they become the next build.

## Items

### UX-0001 — Verifier notes UX hardening (template + export-time validation)
- Template insertion (Goal / Method refs / Evidence inputs / Spot checks / Decision / Uncertainties / Next)
- Character counter + min/max
- Enforce required only on Export/Finalize (not while exploring)
- Light prefill from run context (method/version/runId, itemsCount, linkedRulesCount)
