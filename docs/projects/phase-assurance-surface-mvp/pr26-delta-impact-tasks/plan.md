# PR26 — Delta → Impact → Tasks pipeline

Scope:
- Add Delta/Impact/Tasks fields per Verify run bundle (local persistence).
- Restore via Run history load.
- Include in exported snapshot.

Non-goals:
- Verifier minutes UX hardening (template/limits).
- Backend sync, GitHub issues API.
