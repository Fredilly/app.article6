# Verify rail: Run | Evidence + sticky actions

Scope:
- Add Run/Evidence mode toggle in Verify right rail.
- Sticky command bar for primary actions.
- Move Upload AOI to top of Evidence mode.
- Collapse long-tail panels by default.

Non-goals:
- Feature changes to verifier data model.
- New backend sync or new workflow concepts.
